---
name: cell-cns-figure
description: Create, polish, and review Cell/Nature/Science-style scientific figures directly in Codex. Draw quantitative charts from supplied data with local Python and generate mechanism diagrams, conceptual illustrations, and graphical abstracts with the built-in image generation tool. Use for publication figures and multi-panel layouts, including 科研绘图、机制图、图形摘要; does not use third-party generation services.
---

# Cell_CNS_Figure · Codex 原生版

直接使用 Codex 的推理、内置图像生成和本地绘图能力制作科研图。保留用户的科学结论、数据和明确指定的设计选择。

## 工具选择

- **真实数据图**：散点、曲线、箱线、热图、森林图等，用本地 Python / Matplotlib 绘制；按需使用已安装的 NumPy、pandas、Seaborn。数值、样本、误差线和统计标注必须来自用户数据或可复现计算，不能用图像生成工具绘制真实数据。
- **科研示意图**：机制图、概念图、图形摘要和插画，使用 Codex 内置 `image_gen` 工具；使用时遵循可用的 `imagegen` skill。用当前环境实际提供的工具名与参数，不编造 API、模型选项或保存路径参数。
- **可编辑矢量示意图**：用户明确要求 SVG、可编辑标签或精确流程关系时，可用本地 SVG / Matplotlib 构建。混合图分别生成数据与插画面板，再本地排版；需要可编辑的图中文字时，优先保留为独立的矢量文字层。

本 Skill 不调用小描、OpenRouter、第三方代理、外部生成连接器或单独的 OpenAI API 客户端，不读取密钥、查余额或购买额度。内置图像工具不可用时，说明实际限制；适合矢量实现的任务可继续本地制作，不能悄悄切换服务或索要 API Key。原生图像生成仍受当前 Codex 账户的正常使用限制，不承诺永久免费或完全离线。

## 从需求到成图

先从用户提供的内容中提取核心结论、图中实体和关系、数据来源、输出用途，以及明确指定的尺寸、语言和格式。信息足够就直接制作；只询问影响科学正确性或使任务无法执行的缺失信息，不机械要求布局图 A / 高级图 B 的两阶段确认。

只有摘要、没有数值数据时，默认可以制作基于摘要的机制或概念示意图；如用户要求定量图，应取得真实数据。用户明确要求模拟示例时，清楚标注“模拟数据”，并保存生成方法。参考图只提供用户指定的结构或风格参考，不能将参考图中的实验结果当成用户结果。

选择能够支持核心结论的布局。多面板各自回答一个必要的问题，统一类别颜色、字体、单位与符号；按证据顺序排列，避免为凑面板数量重复信息。沿用用户指定的配色和布局，未指定时采用克制的期刊风格。

### 科学内容与视觉风格

- 机制箭头的方向、促进/抑制符号、上下游关系、条件与时间顺序应有来源。区分实验证实、假说和示意，不把相关性画成已证实因果，不增加摘要中没有依据的机制。
- 默认白色背景、清晰轮廓、少量语义颜色、充足留白；避免装饰性渐变、伪 3D 和无意义图标。色彩之外用形状、线型或直接标签辅助辨认。
- 数据图使用 Arial / Helvetica 或可用的相近字体，保留单位、样本量与必要图例。误差线注明 SD / SEM / CI；显著性标注有相应检验和比较，不能凭视觉添加星号。
- 所有字体和线宽按最终输出尺寸检查。单栏约 89 mm、双栏约 180 mm 可作初始设计尺寸，不声称适用于所有期刊；需要满足具体投稿要求时查阅目标期刊官方指南。

### 内置图像生成

将用户内容组织为清晰提示词：科学信息、实体与关系、布局、风格、逐字标签，以及必须保持的内容。保存最终提示词，方便后续针对性修改。标签较多时缩短文案，避免给每个元素添加说明段落。

编辑本地图像前先查看图像，再通过工具支持的引用方式传入；对于修改请求，明确目标变化与应保持的科学关系。生成后检查实际图片中的文字、箭头、符号、元素数量和布局。发现影响结论的错误时针对性修正，不能把仅提交工具任务当成交付。

### 本地数据绘图

在当前项目环境运行代码，优先复用已有绘图依赖；确有需要时只补充与本任务相关的包。绘图前核对字段、分组、单位、缺失值与重复记录；不能静默删除数据或将细胞/技术重复当作独立生物学重复。

尺寸、字号、调色板与导出设置见 [references/plotting.md](references/plotting.md)，制作或修改定量图时读取。保存绘图源代码与关键数据变换，确保图件可复现。

## 检查与交付

查看实际渲染的输出，检查文字重叠、裁切、箭头关系、色彩含义和最终尺寸的可读性。修改现有图时只改变请求范围，使用版本化文件名，除非用户要求覆盖。

- 数据图交付 SVG 或 PDF 矢量文件，以及 PNG 预览和绘图代码。SVG 保留文字对象；密集数据层可栅格化，坐标轴、标签与图例保持矢量。
- 内置工具生成的图像交付真实 PNG 等输出并直接展示。不能将 PNG 装进 SVG/PDF 后称其为可编辑矢量图，不能把增加 DPI 元数据当作提高图像细节。
- 用户要求 PPTX 时，使用可用的演示文稿能力创建，说明哪些对象可编辑；插入图片不等于机制元素全部可编辑。
- 交付图像工具实际结果或已验证存在的本地文件，不仅返回提示词或计划。遵守当前工作区输出约定：中间文件放 `work/`，最终文件放 `outputs/` 或用户指定位置；必要时将内置生成工具返回的本地结果复制到交付目录。

最终回复展示图件并链接成品，用简短文字指出关键内容和实际存在的限制。不附加广告、品牌致谢、服务报价或余额信息。只安装、更新或检查本 Skill 时，仅处理 Skill 本身。
